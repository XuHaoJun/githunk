# Lazygit compatibility (githunk v0.1)

This is a compatibility guide, not a claim of complete lazygit parity. The reference is the vendored lazygit at `learn-projects/lazygit/docs/keybindings/Keybindings_en.md` and `pkg/config/user_config.go`, especially its Global keybindings, List panel navigation, Files, Local branches, Commits, and Main panel sections. The status matrix below is the authoritative statement for this v0.1 core UI and navigation slice.

Statuses: `compatible`, `partially compatible`, `not yet implemented`, `githunk review extension`, `blocked by an identified external limitation`. Rows 18-20 and 24-28 are the feature-gap inventory: they enumerate, panel by panel against the reference keybindings, what lazygit does that githunk does not, so an unlisted reference key can be read as an omission from this document rather than as silent parity. Every subsequent parity-program subsystem not listed as compatible is `not yet implemented`, never `intentionally changed`.

Githunk review extensions in this slice are exactly four: Main selection/copy, the lower-right review area, draggable splitters, and the command log's failed-command output block.

## Status matrix

| # | Feature | Status | Notes | Reference |
|---|---------|--------|-------|-----------|
| 1 | Numbered focus `0`–`5` and pane navigation `h`/`l`, `Tab`/`Shift+Tab` | compatible | `0` Main, `1` Status, `2` Files, `3` Branches, `4` Commits, `5` Stash; `h`/`l`/`Tab` cycle in same order; `q`/`Ctrl+C` quit | `user_config.go:1002,1020-1023`, Keybindings lines 31,38-53 |
| 2 | Full-row list selection | compatible | Focused row uses full-row `bgBlue` highlight; no `>`/`▸` cursor glyph; unfocused retains identity but removes highlight | `pkg/gui/presentation` selection contract |
| 3 | Commit graph, author and time | compatible | Graph lane topology (linear/branch/merge/convergence) via `parentOids`, abbreviated hash, subject, author name, relative time where width permits; width-priority truncation. The hash carries `getHashColor`'s status colours — red unpushed, yellow pushed, green merged — from the same `rev-list` reachability pair lazygit uses (`commit_loader.go:104-124,543-558`) against the `master`/`main` refs that exist here (`main_branches.go:89-116`); `theme.DiffTerminalColor` for the diffed commit and the cherry-picked style are `not yet implemented`, as githunk has neither diff-compare mode nor a cherry-pick basket | `pkg/gui/presentation/commits.go:477-512` |
| 4 | Commit metadata / stat / patch preview | compatible | Main shows commit `Author`/`AuthorDate`/`Commit` metadata, `file changed` stat, then full `diff --git` patch; no patch-size threshold; file list only via explicit drill-down | `pkg/gui/controllers/helpers/diff_helper.go`, `git show --stat -p -m` |
| 5 | CommitFiles transient drill-down | compatible | `Enter`/`double-click` in window 4 opens CommitFiles child with `commit-files` view; `Escape` restores parent OID and commit preview; browsing never changes `reviewTarget`/`branchReviewTarget`; global `MainPreviewGate` suppresses stale previews | `pkg/gui/controllers/switch_to_diff_files_controller.go` |
| 6 | Branches / Remotes / Tags tabs | compatible | Window 3 hosts `Local Branches`, `Remotes`, `Tags` tabs; title renders `Local Branches \| Remotes \| Tags` with active cyan; `[`/`]` wraparounds only when a multi-tab side window is focused; each tab retains stable selection and scroll; `[`/`]` do not change `reviewTarget` scope when Files or Main focused | `user_config.go:1059-1060 PrevTab/NextTab` |
| 7 | RemoteBranches transient drill-down | compatible | `Enter` on a `remote:` row opens `RemoteBranches` child listing `remote/branch` refs with target short OID; `Escape` restores Remotes parent selection; `[`/`]` inside child first leaves child then cycles parent tabs | Window 3 child model |
| 7a | Local branch commits child | compatible | `Enter` on a Local Branches row loads that ref's commits into a transient panel-3 child titled `Commits (<branch>)`; Main previews the selected commit; `Escape` restores the branch row and graph; panel 4's local commits and the review target remain unchanged | `switch_to_sub_commits_controller.go:45-73`, `src/ui/root-view.ts` |
| 8 | Stash folding | compatible | Normal layout at `≥28` side rows: Stash is `3` rows when not current side window, weight `1` when focused; at `21–27` non-current folds to `3`; below `21` folds to `1`; current side window absorbs remainder; log/hints row does not reduce side height | `window_arrangement_helper.go` |
| 9 | Mouse row selection and double-click | compatible | Single left click inside clipped `[screenX, screenX+width)` x `[screenY, screenY+height)` captures `text.scrollY+event.y - screenY` row's stable ID before focus; focuses owning window, selects via `selectListRow`, reveals highlight, runs preview gate; border/title/clipped/empty not selectable; blank list space focuses without changing selection; double-click `≤400ms` + `≤1` cell same view/stable ID invokes `Enter` | List parity design §6.4 |
| 10 | Pointer-local wheel scrolling | compatible | Wheel `up`/`down` over pane content, border/title, or scrollbar scrolls only that pane's viewport by `2` rows per delta; does not change focus or selection; wheel over scrollbar is pane-wheel not press; wheel over splitter is consumed no-op; one event consumed by exactly one pane | `onMouseScroll` wiring |
| 11 | Interactive scrollbar | compatible | Visible only when `scrollHeight > viewport`; track click jumps to ratio position; thumb drag changes `text.scrollY` via ratio; `syncVerticalScrollbar` on keyboard, wheel, reveal, refresh, resize, track, drag; press/drag does not change focus or select rows; does not fall through to Main selection or splitter | `ScrollBarRenderable` + `SliderRenderable` |
| 12 | Main selection / copy | githunk review extension | Main `TextRenderable` native selection maps via `DiffDocument.rendered.displayToRaw` + `segments` to UTF-16 offsets (handles wide/combining); `Ctrl+O` copies exact patch bytes, `y` opens copy menu; never includes left pane text | Extension 1 of 4 |
| 13 | Command log content, colours, autoscroll, keybindings, `@` menu, sizing, default visibility | compatible | Action/command stream per `pkg/gui/command_log_panel.go:25-68`, including the yellow action / default-or-magenta command styling and the startup header with a random tip drawn from a 13-of-~30 documented subset (`src/app/command-log-tips.ts`'s block comment records the exclusions); `readOnly` implies `dontLog` (`src/git/runner.ts:84-88`), reproducing lazygit's 76 `DontLog()` call sites (a naive `grep -c` finds 80; 4 are the declaration and its own comments, `cmd_obj.go:19,118,125,130`), so background loaders never appear; autoscroll arms on every logged write (`CommandLog.autoscrollArms`) and clears on manual scroll; `,`/`.`/`<`/`>` page/jump the log in its own binding context; `@` opens the Command log menu (`t` toggle, `f` transient focus) per `extras_panel.go:12-46`; `getExtrasWindowSize` sizing per `window_arrangement_helper.go:403-417`; visible by default; persisted **inverted**, as `commandLogHidden` (`src/ui/ui-state-store.ts`'s on-disk `version: 2` shape), mirroring lazygit's own boundary between the in-memory `gui.ShowExtrasWindow` (`pkg/gui/gui.go:190`) and the on-disk `AppState.HideCommandLog` (`pkg/config/app_config.go:858`, inverted at `gui.go:523` and `extras_panel.go:26`) — `getDefaultAppState()`'s bare `&AppState{}` (`app_config.go:861-862`) means an absent/older state reads back as shown, so githunk migrates a `version: 1` file (which persisted the positive `commandLogVisible`, from before the row-13 default flipped to shown) the same way: `commandLogHidden`'s absence marks it pre-migration, `commandLogVisible`/`commandLogHeight` reset to the current defaults, and only `sidePanelRatio` survives | — |
| 13a | Lower-right review area | githunk review extension | Fixed lower-right region hosting review status above the command log; height controlled via `logHeight` and layout; independent scrolling; does not reduce left side-section height calculation | Extension 2 of 4 |
| 13b | Failed-command output in the log | githunk review extension | lazygit raises an error popup and logs nothing extra for a non-streamed failure; githunk has no popup, so PRD §6.7's "failures stay inspectable" is met by putting stderr under the same magenta `Git output:` heading a streamed command uses | Extension 3 of 4 |
| 14 | Splitters | githunk review extension | Vertical splitter between side and Main, horizontal splitter for the lower-right region — the mouse-driven equivalent of lazygit's `gui.commandLogSize` config (`pkg/config/user_config.go:191`); `1`px hit, hover highlight; drag captures exclusive `gestureOwner` (vertical/horizontal/scrollbar/Main-selection) and continues outside original hit target; release clears owner; vertical wins on degenerate overlap | Extension 4 of 4 |
| 15 | Files window tabs (Worktrees / Submodules) | compatible | Panel 2 hosts lazygit's `{files, worktrees, submodules}` tab group (`user_config.go:872`); the Files tab renders as a compressed directory tree, `[`/`]`/click cycle or activate tabs, and the Worktrees/Submodules tabs preview the selection in Main (`worktrees_controller.go:80-118`, `submodules_controller.go:107-127` GetOnRenderToMain equivalents). Both tabs are read-only: Enter is inert because switching worktree or entering a submodule changes the repository root wholesale; their write operations stay under row 18 | Program sub-project 2 |
| 16 | Reflog tab in Commits window | compatible | Panel 4 hosts lazygit's `{commits, reflog}` tabs (`user_config.go:874`); rows render short hash + subject per `reflog_commits.go:72`, an empty reflog shows "No reflog history", and selecting an entry previews `git show <hash>` through the Commits tab's own inspection path (`reflog_commits_controller.go:40-52`). Enter is deliberately inert: lazygit gives ReflogCommits SwitchToSubCommitsController, not the commit-files drill-down, and githunk has no sub-commits panel (`pkg/gui/controllers.go:240-249`) | Program sub-project 4 |
| 17 | Files, staging and patch-building parity | partially compatible | Working-tree scope views (`all`/`staged`/`unstaged`, cycled with `[`/`]` in Main per PRD §8.1), file-level stage/unstage (`Space`, `a` for every file, directory toggle follows `files_controller.go:509`), hunk/line staging and discarding from Main within a scoped target, and commit/amend creation with lazygit's staged-content semantics: `c`/`A` work from any scope whenever the index holds anything, and an empty index offers the second-press "stage everything" retry mirroring `WithEnsureCommittableFiles`. The commit message UI is a centered popup with separate summary/description editors and lazygit's Enter/Tab/Ctrl+Enter/Esc controls, and `d` opens lazygit's discard-options menu rather than discarding directly (`src/ui/root-view.ts:1545-1575`). Still missing in this panel: `D` reset-working-tree options, `g` upstream reset options, `i` ignore/exclude file, `Ctrl+B` filter files by status, `Ctrl+F` find base commit for fixup, `w` commit without the pre-commit hook, `C` commit using the git editor, `E` edit hunk in an external editor, `Tab` staged/unstaged view switch (the `[`/`]` scope ring is the near-equivalent), `y`/`Ctrl+O` copy path, the Secondary view lazygit shows alongside the staging panel, and custom patch options (row 27) | Program sub-project 2; `pkg/gui/controllers/helpers/working_tree_helper.go:229-258`, `pkg/gui/controllers/commit_message_controller.go:30-57`, `src/ui/commit-message-panel.ts` |
| 18 | Branches, remotes, tags, worktrees, submodules operations | partially compatible | Local branches are writable: `space` checkout (a remote-branch row creates the local tracking branch), `n` create, `d` delete options, `r` rename, and `f` fetch for a selected remote row (`src/ui/bindings.ts:371-377` -> `src/git/branches.ts:182-283`), each serialised through `runUiMutation` and confirmed through the centered popups of `src/ui/branch-dialogs.ts`. Divergence to note: lazygit's Local-branches `f` is Fast-forward, while githunk's `f` is available only on a remote row and otherwise falls through to the global fetch. Not yet implemented — Local branches `r` rebase, `M` merge, `f` fast-forward, `u` upstream options, `g` reset, `c` checkout-by-name, `-` previous branch, `F` force checkout, `T` new tag, `s` sort order, `i` git-flow, `N` move commits to new branch, `w` new worktree, `o`/`O`/`G`/`Ctrl+Y` pull-request create/open/copy-URL (only `gh pr list` for the branch-row icon exists, `src/git/github.ts:115-129`), `Ctrl+O` copy branch name; Remotes `n`/`d`/`e`/`F`; Remote branches `u` set-as-upstream, `M`, `r`, `g`, `s`; every Tags write (`n`/`d`/`P`/`space`/`g`); every Worktrees write (`n`/`space`/`o`/`d` — `removeWorktree`/`detachWorktree` exist at `src/git/worktrees.ts:274-282` but no binding reaches them); every Submodules write (`enter`/`d`/`u`/`n`/`e`/`i`/`b`) | Program sub-project 3; Keybindings §Local branches (line 172), §Remotes (314), §Remote branches (295), §Tags (393), §Worktrees (409), §Submodules (379) |
| 19 | Commits, reflog, rebase, cherry-pick, bisect operations | not yet implemented | The Commits panel is read-only beyond row 5's `enter` drill-down and row 20's `e` edit-file. Absent: `s` squash, `f` fixup, `c` set fixup message, `r`/`R` reword, `d` drop, `e` edit-commit, `i` start interactive rebase, `p` pick, `F` create fixup, `S` apply fixups, `Ctrl+J`/`Ctrl+K` move commit, `B` mark base commit, `C`/`V`/`Ctrl+R` the cherry-pick basket, `t` revert, `g` reset, `T` tag commit, `a` amend attribute, `space` detached checkout, `n` new branch off commit, `N` move commits to new branch, `b` bisect, `Ctrl+L` log options, `y`/`Ctrl+O` copy commit attribute, `o`/`G` open commit/PR in browser, `*` select commits of current branch, `w` new worktree. `src/git` contains no rebase, cherry-pick, revert, reset or bisect code path at all, and `A` amend is HEAD-only (`src/git/commit-mutations.ts:64-76`) rather than lazygit's rebase-amend of an arbitrary commit. Reflog stays view-only by design (row 16); the sub-commits child of row 7a carries none of §Sub-commits' operations | Program sub-project 4; Keybindings §Commits (line 82), §Reflog (275), §Sub-commits (359) |
| 20 | Stash, sync, undo, custom commands, external tools | partially compatible | Compatible: `s` stash-create with the include-untracked choice, `space` apply, `g` pop, `d` drop, `enter` inspect (`src/ui/bindings.ts:391-396` -> `src/git/stash.ts:35-68`); `f` fetch, `p` pull, `P` push with lazygit's upstream-required prompt (`src/ui/bindings.ts:320-323` -> `src/git/sync.ts:76-108`); `e` edit for Files / Main (at hunk) / Commit Files via `src/git/editor.ts` (preset-aware `{{filename}}`/`{{line}}`, `GITHUNK_EDIT`/`GITHUNK_EDIT_AT_LINE` overrides, suspend-aware) and `src/ui/root-view.ts:actionEditFile`. Not yet implemented — `z`/`Z` undo/redo (no reflog-driven inverse-command table), Files `S` stash-options variants (stash staged only / unstaged only), Stash `n` new branch from stash, `r` rename stash, `w` new worktree, `:` shell command, `o` open in the default application, `Ctrl+T` difftool, custom commands, `Ctrl+R` recent repos | Program sub-project 5; `src/git/editor.ts:resolveEditCommand`, `pkg/config/editor_presets.go`, `pkg/gui/controllers/files_controller.go:91`; Keybindings §Stash (line 334), §Files (132) |
| 21 | Configuration, keybinding, theme compatibility | not yet implemented | `user_config.go` full keybinding/theme compatibility and config file loading remain not implemented; the shipped default color semantics now preserve lazygit's ANSI indexed/default behavior through `src/ui/theme.ts` | Program sub-project 6 |
| 22 | Other reference Global keybindings | not yet implemented | `Ctrl+R` recent repo, `{`/`}` diff context size, `(`/`)` rename similarity threshold, `\|`/`\` cycle diff renderers, `:` shell command, `Ctrl+W` toggle whitespace, `Ctrl+Z` suspend, `Alt+Shift+C` edit config file, `Ctrl+T` difftool, `o` open in default application, `Ctrl+S` filter options, `W`/`Ctrl+E` diffing options. `W`/`Ctrl+E` is the entry point PRD §19's arbitrary compare base needs. (An earlier revision of this row transposed the two pairs: `{`/`}` is diff context size and `(`/`)` is rename similarity, per §Global.) | Keybindings §Global (line 5) |
| 23 | Main preview generation gate | compatible | Single monotonic generation across Files/branches/tags/commits/commit-files/stash; synchronous install increments generation; stale async result discarded when generation/source/stableId mismatch; loading retains prior content | `MainPreviewGate` |
| 24 | Range select (`v`, `shift+up`/`shift+down`) | not yet implemented | lazygit gives every list a keyboard range selection that the panel operations then act on in bulk; githunk's `ListState` tracks a single index and no `Action` claims `v` (`src/ui/list-view.ts`, `src/ui/bindings.ts:6-31`). Main panel staging/patch-building `v` and `a` (hunk-vs-line selection mode) are absent for the same reason; row 12's Main selection is the mouse/OSC52 review extension, not lazygit's keyboard range | Keybindings §List panel navigation (line 38), §Main panel (staging) (245) |
| 25 | In-view search and filter (`/`) | compatible | `/` opens a per-context filter/search prompt exactly as lazygit's `SearchHelper.OpenFilterPrompt` / `OpenSearchPrompt` do (`pkg/gui/controllers/helpers/search_helper.go:32-64`, `pkg/gui/controllers/filter_controller.go:39`, `pkg/gui/controllers/search_controller.go:39`). Filterable contexts (`Files`, `Worktrees`, `Submodules`, `Local Branches`, `Remotes`, `Remote Branches`, `Tags`, `Reflog`, `Stash`, `Commit Files` via `FilteredListViewModel` `pkg/gui/context/filtered_list_view_model.go:27`) live-filter the list via `filterItems` (`src/app/filter.ts:18-26`) and keep the query in `RootView.filters` per `pane:tab` (`src/ui/root-view.ts:315-318, 949-953, 2738-2763`). Searchable contexts (`Commits` / `SubCommits` `pkg/gui/context/local_commits_context.go:36` and `Main` `pkg/gui/context/main_context.go:13`) keep the full list, highlight the query (`src/ui/root-view.ts:3164-3183` search highlight, `src/ui/panes/commits-pane.ts:68-76` searchable hash/subject) and support `n`/`N` next/prev match (`pkg/gocui/gui.go:303`, `src/ui/bindings.ts:384-385`, `src/ui/root-view.ts:2765-2895`). `Esc` clears the filter/search whether the prompt is open or the query is persisted (`SearchHelper.Cancel` `pkg/gui/controllers/helpers/search_helper.go:206-221`, `src/ui/root-view.ts:2518-2552`), `Enter` confirms and keeps it. The prompt shows `Filter: <query>` or `Search: <query>` in the pane's `bottomTitle` (`src/ui/root-view.ts:1363-1381`), matching lazygit's `SearchPrefix`/`FilterPrefix` (`pkg/i18n/english.go:2040-2041`). `Ctrl+B` and `Ctrl+S` remain the only `/`-adjacent options not yet implemented. | Keybindings §List panel navigation (line 38) plus each panel's trailing `/` row |
| 26 | Merge conflicts | not yet implemented | Conflicts are detected and surfaced only: `git status`'s `U` states set `ReviewFile.conflicted` (`src/git/status.ts:41,57,75`), the file tree sorts conflicted files to the top exactly as lazygit does (`src/ui/file-tree.ts:240-246`), and line-level staging/discarding is refused on them (`src/git/mutations.ts:67,84`, `src/ui/panes/main-pane.ts:123`). None of the resolution surface exists: Files `M` merge-conflict options, the whole Main panel (merging) mode (`space` pick hunk, `b` pick both hunks, `h`/`l` previous/next conflict, `z` undo resolution), or the global `m` abort/continue/skip for an in-progress merge or rebase | Keybindings §Files (line 132), §Main panel (merging) (203), §Global (5) |
| 27 | Custom patch builder | not yet implemented | `Ctrl+P` patch options, the Commit files `space`/`a`/`enter` patch-inclusion toggles and the entire Main panel (patch building) mode are absent, along with Commit files `c` checkout-file and `d` discard-this-commit's-changes. githunk's `parse.ts` -> `render.ts` -> `selection.ts` -> `transform.ts` pipeline already produces exact patch bytes for the `patch`/`hunk`/`file` copy modes, so of the unimplemented lazygit subsystems this is the one closest to githunk's own product core | Keybindings §Main panel (patch building) (line 229), §Commit files (55) |
| 28 | Status panel operations | not yet implemented | Panel 1 renders the review target, branch, base and review summary (`src/ui/panes/status-pane.ts`) and is titled `1 Status / Review`; lazygit's Status-panel operations — `e` edit config file, `u` check for update, `enter` switch to a recent repo, `a`/`A` show/cycle all branch logs — are all absent | Keybindings §Status (line 348) |

Additional notes:

- `[` / `]` cycle tabs when the focused window owns multiple tabs (windows 2/3/4 and transient children). In Main (0) — the only tabless window — they cycle the working-tree scope ring `all → staged → unstaged` (PRD §8.1) while the focused review target is a working tree; side-window tab cycling never changes `reviewTarget` or working-tree scope.
- Commit browsing (cursor move, Enter, Escape) never changes `reviewTarget`; `reviewTarget` changes only via explicit mode actions (`b`/`w`, file operations, branch checkout).
- Main generation has no patch-size threshold; large patches flow through the same gate as small ones.
- Wheel and scrollbar are explicitly wired via `onMouseScroll` and `ScrollBarRenderable.onChange`; handlers do not merely stop propagation.
- Exactly one gesture owner is captured at a time (vertical splitter, horizontal splitter, one scrollbar, Main-selection, or none); hit-test precedence is scrollbar, vertical splitter, horizontal splitter, then pane content; release/cancel clears ownership.
- External staging/unstaging: githunk watches the resolved Git index directory and debounces `index`/`index.lock` events, so cross-process index changes refresh before the 10-second fallback; this is intentionally more responsive than lazygit's polling-only implementation.

Verification for this slice is the repository-backed acceptance test `tests/acceptance/lazygit-core-ui.test.ts` (fixture with base/side/main/merge, light/annotated tags, stash, rename-before/after, staged/unstaged, fetch/push bare remotes) plus `bun run typecheck` and `bun test` and a PTY TUI smoke exercising `4`/click/wheel/`Enter`/file click/`Escape`/`3`/`[`/`]`/remote/`5`/splitters/scrollbar/Main Unicode copy.
